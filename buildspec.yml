version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install --legacy-peer-deps

  build:
    commands:
      - npm run build

artifacts:
  files:
     - .next/**/*
     - public/**/*
     - package.json
     - next.config.ts
